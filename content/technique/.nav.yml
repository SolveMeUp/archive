# 카테고리 한글(영문) 라벨.
# 두 글은 파일명 자동정렬이 학습 흐름과 어긋난다('-'<'.'이라 sliding-window-maximum이
# 기본 sliding-window보다 앞으로). 그래서 교육 순서를 명시 고정한다.
# 기본 슬라이딩 윈도우(구간 합) → 단조 큐 응용 슬라이딩 윈도우(구간 최대/최소).
# 적은 글은 이 순서, 나머지(새 글)는 자동으로 뒤에 붙는다(append_unmatched).
title: 테크닉 (Technique)
nav:
  - sliding-window.md
  - sliding-window-maximum.md
